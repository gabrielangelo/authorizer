import Config

# Do not include metadata nor timestamps in development logs
# config :logger, :console,
#   format: "$metadata[$level] $message\n",
#   metadata: [:error],
#   level: "LOG_LEVEL" |> System.fetch_env!() |> String.to_atom()
